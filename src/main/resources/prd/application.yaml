spring:
  application:
    name: 'iTimer-Server'
  profiles:
    active: 'db'
server:
  port: 8080
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.jim.itimerserver.entity