spring:
  datasource:
    url: jdbc:h2:file:/home/jim/DevCodes/iTimer-Server/devdb/h2db;IFEXISTS=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true